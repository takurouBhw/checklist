type ChecklistWork = {
    id: number;
    category_id: number;
    checklist_id: number;
    user_id: string;
    task_title: string;
    started_at: string;
    ended_at: string;
};

export default ChecklistWork;
